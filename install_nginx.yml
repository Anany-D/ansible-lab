---
- hosts: local
  become: yes

  tasks:
   - name: Ensure Nginx exists
     apt:
         name: nginx
         state: present
   - name: Ensure Nginx running
     service:
           name: nginx
           state: started 

